<h1>Bem-vindo ao restaurante: {{restaurant.nome}}, aberto {{restaurant.hoursOpen}} horas</h1>

<p id="explanation">O melhores pratos gourmet da região de Narnia</p>
<ul>
  <h4>{{availableItemsTotal}} de {{menuLength}} pratos disponíveis</h4>
    {{#each model as |food|}}
      {{#if food.isAvaliable}}
        <li>
          {{#link-to 'food' food.name}}{{food.name}}!{{/link-to}}
        <button {{action 'makeUnavaliable' food}}> Desabilitar</button>
        </li>
      {{else}}
        <li class = "not-avaliable">{{food.name}}
        <button {{action 'makeAvaliable' food}}> Habilitar</button>
        <button {{action 'destroyItem' food}}> Destruir </button>
      </li>
      {{/if}}
    {{/each}}
</ul>
<hr>
<p>Adicionar prato</p>
{{input value = newItem}}
<button {{action 'saveNewItem'}}>Salvar</button>
